# Used by docker-compose.yml (copy to ".env" in this same folder, next to
# docker-compose.yml, and compose will pick it up automatically).
GEMINI_API_KEY=your_google_ai_studio_api_key
GEMINI_MODEL=gemini-2.0-flash
